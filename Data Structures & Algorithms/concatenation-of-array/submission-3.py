class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        a=[]
        for i in range(len(nums)):
            a.append(nums[i])
        for i in range(len(nums)):
            a.append(nums[i])
        return a